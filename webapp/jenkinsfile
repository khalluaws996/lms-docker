pipeline {
    agent any

    stages {
        stage('Build Lms') {
            steps {
                echo 'Lms Build'
                sh 'cd webapp && npm install && npm run build'
            }
        }

        stage('Publish Artifacts') {
            steps {
                script {
                    echo "Publishing artifacts"       
                    def packageJSON = readJSON file: 'webapp/package.json'
                    def packageJSONVersion = packageJSON.version 
                    
                    sh 'ls -l webapp/dist' // Debugging: List contents of dist directory
                    
                    sh 'which zip' // Debugging: Check availability of zip command
                    sh 'zip -r webapp/dist-${packageJSONVersion}.zip webapp/dist/*'
                    sh "curl -v -u admin:admin123 --upload-file webapp/dist-${packageJSONVersion}.zip http://54.237.77.36:8081/repository/lms/"
                }
            }
        }
    }
}
